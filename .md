# Why use Hyperf

1、在工作中使用到
2、想尝试微服务
3、想参与开源项目的维护
4、国内开发组
5、非FPM框架，想尝试

# 但是基本没有公司用，不好找工作已经直接被kill掉了

# think about
```
就是为了找工作，赚钱。Hyperf确实很好，可是laravel更好找工作。
以后可能不会从事底层开发吧。但是学C也没得影响。学了C学其他的还是快。
```

# 项目成果转换
1. 版本更新解决方案
2. 安装部署解决方案
3. 应用隔离解决方案
4. 分布式锁解决方案
5. 数据一致性解决方案
6. 程序健壮性解决方案
7. 老项目重构解决方案

## 版本更新解决方案
### 整体思路
``` 
我想的: 
文件:通过拉取云端的代码,然后把文件全部遍历,生成json文件,然后使用 md5_file来生成文件的内容的md5
数据库:查询数据库的 information_schema,查询要同步的数据库的所有表信息.包括表名,表字段,引擎,字符集,索引等,生成json文件.路由表,菜单表,权限表则生成insert sql语句

```

实现
```php
<?php
 
```

后续展望:先实现,等实现完了之后。进行改造升级（migrate）
